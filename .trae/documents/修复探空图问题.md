# 修复探空图 is_dry 未定义和标签重叠问题

## 问题分析

### 问题1: `is_dry` 未定义
- **位置**: `sounding_plotter.py` 第396行
- **错误**: 使用了 `is_dry` 变量但没有定义
- **原因**: 代码中定义了 `mid_depression` 用于判断干燥程度，但忘记定义 `is_dry` 变量
- **修复**: 在使用前添加 `is_dry = mid_depression > 15`

### 问题2: 逆温层和0度层标签重叠
- **位置**: `sounding_plotter.py` 第343-344行
- **当前逻辑**: 当 `freezing_hght < inversion_hght`（0度层在逆温层下面）时交换标签位置
- **问题**: 当逆温层在0度层上面时，标签会重叠
- **修复**: 调整交换逻辑，确保两种情况都有足够间距

## 修改计划

### 修改文件
- `sounding_plotter.py`

### 具体修改

#### 1. 定义 is_dry 变量
在第396行之前添加：
```python
is_dry = mid_depression > 15
```

#### 2. 修复标签位置逻辑
当前代码（第324-344行）：
```python
if has_inversion and freezing_hght < inversion_hght:
    y_freeze_label, y_inv_label = y_inv_label, y_freeze_label
```

修改为：根据实际高度关系调整标签位置，确保不重叠
