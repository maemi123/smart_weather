I will implement the History Trend analysis page with the following steps:

### 1. Backend Logic (`history_analyzer.py`)

*   **Add `analyze_trend` method**:
    *   Iterate through all available years in the dataset.
    *   Calculate key annual metrics:
        *   Average Temperature (Yearly)
        *   Total Precipitation (Yearly)
        *   Extreme Heat Days (Max Temp ≥ 35°C)
        *   Extreme Cold Days (Min Temp ≤ 0°C)
        *   Rainy Days (Precip ≥ 0.1mm)
    *   Calculate **Linear Regression Trends** (Slope/Rate of Change) for Temperature and Precipitation to quantify warming/wetting trends (e.g., +0.X°C/decade).
    *   Structure the output as a list of dictionaries (time series data) suitable for frontend JSON consumption.

### 2. Route Logic (`app.py`)

*   **Update `/history/trend` route**:
    *   Call `analyzer.analyze_trend()` to fetch the time series data.
    *   Pass this data to the template.

### 3. Frontend Implementation (`templates/history_trend.html`)

*   **Remove "Under Construction" placeholder**.
*   **Integrate Plotly.js**:
    *   **Chart 1: Temperature Trend**: Line chart showing Annual Avg Temp, with a regression trendline.
    *   **Chart 2: Precipitation Trend**: Bar chart for Total Precipitation + Line for Rainy Days.
    *   **Chart 3: Extreme Weather Days**: Stacked or Multi-line chart showing Heat Days vs. Cold Days.
*   **Add "Climate Summary" Section**:
    *   Display calculated rates (e.g., "Warming rate: +0.4°C/10 years").
    *   Brief text interpretation of the trends.

### Implementation Steps

1.  Modify `history_analyzer.py` to add `analyze_trend` method.
2.  Modify `app.py` to update the route.
3.  Rewrite `templates/history_trend.html` to visualize the data.
