I will implement the Sounding Data Visualization and Analysis Module as follows:

### 1. Backend Implementation

*   **Install Dependencies**: Add `MetPy` and `scipy` to the project for advanced plotting and calculations.
*   **Enhance `SoundingDataParser`**: Modify `sounding_parser.py` to save data to a structured `data/upperair/` directory instead of the root folder.
*   **Create `SoundingPlotter` (`sounding_plotter.py`)**:
    *   **T-lnP & Skew-T**: Use MetPy to generate professional thermodynamic diagrams.
    *   **Wind Profile**: Create a vertical wind profile chart using Matplotlib/MetPy.
    *   **Cartoon/Simple Plot**: Generate a simplified SVG/PNG diagram summarizing stability (e.g., using icons for stable/unstable layers).
*   **Create `SoundingAnalyzer` (`sounding_analyzer.py`)**:
    *   **Parameter Calculation**: Use MetPy to calculate missing parameters if not provided by the raw data (e.g., LCL, LFC, EL, bulk shear).
    *   **AI Analysis**: Integrate with the DeepSeek API to generate the professional analysis report based on the calculated indices.
    *   **Risk Matrix**: Implement logic to assess severe weather risks based on CAPE and Shear values.
*   **Route Logic (`app.py`)**:
    *   Add `/upperair` routes to handle main page rendering, plot serving, and data/analysis API endpoints.

### 2. Frontend Implementation

*   **Template (`templates/upperair.html`)**:
    *   Create a responsive layout with a sidebar for plot selection and a main area for display.
    *   Implement "Plot Type Selection" as radio buttons or a list group.
    *   Implement "Time Selection" (Latest vs. History).
    *   **Analysis Panel**: Display key parameters (CAPE, CIN, etc.) and the AI Analysis report.
    *   **Collapsible Risk Assessment**: A Bootstrap accordion for the severe weather risk matrix.
*   **JavaScript (`static/js/upperair.js`)**:
    *   Handle plot switching (fetching and updating the image source).
    *   Fetch and render the AI analysis report asynchronously.
    *   Handle historical data fetching.

### 3. Execution Plan

1.  **Environment**: Install `metpy` and `scipy`.
2.  **Backend Logic**: Implement `SoundingPlotter` and `SoundingAnalyzer`.
3.  **Routes**: Update `app.py`.
4.  **Frontend**: Create HTML template and JS.
5.  **Integration**: Verify the end-to-end flow from data fetching to visualization.
