I will fix the issue where emojis (like 💧, ❄️, 🏃‍♂️) are rendering as square boxes in the Matplotlib charts.

### 1. Diagnosis
The issue is that the standard `Microsoft YaHei` font, while great for Chinese, often lacks the colored emoji glyphs or support for newer Unicode emojis in the way Matplotlib's default backend renders them. Matplotlib doesn't support color fonts (like Segoe UI Emoji) by default without specialized backends or font fallback handling.

### 2. Solution: Use `Segoe UI Emoji` for Icons
To ensure emojis render correctly (even if monochrome), I need to explicitly tell Matplotlib to use a font that supports them for the specific text elements containing emojis. On Windows, `Segoe UI Emoji` is the standard font for this.

**Strategy:**
*   Modify `_plot_simple` in `sounding_plotter.py`.
*   For every `ax.text()` call that includes an emoji, I will explicitly set the `fontname` or `fontfamily` parameter to use `"Segoe UI Emoji"`.
*   However, mixing Chinese text and Emojis in the same string with a single font setting is tricky because `Segoe UI Emoji` might not support Chinese well, and `Microsoft YaHei` doesn't support all emojis.
*   **Better Approach**: I will split the text or use a font fallback list that prioritizes Emoji font for symbols. Matplotlib's `font.sans-serif` list handles fallback, but sometimes it's flaky for specific glyphs.
*   **Refined Strategy**: Since I'm on Windows, I'll add `Segoe UI Emoji` to the global font list *before* generic fallbacks but *after* Chinese fonts, OR apply it specifically to the icon text elements.
*   Given the user reported "square boxes", it's likely `Microsoft YaHei` just doesn't have those specific glyphs.

**Implementation Plan:**
1.  **Global Font Update**: Add `Segoe UI Emoji` to the `plt.rcParams['font.sans-serif']` list in `__init__`.
2.  **Specific Font Override**: For the text elements that are *purely* icons (like the water droplets/snowflakes), I will force `fontname="Segoe UI Emoji"`.
3.  **Mixed Text**: For text like "🏃‍♂️ 心率过快", I will try to use a font list that includes both. If that fails, I might need to split the string (draw emoji with Emoji font, text with Chinese font), but that's complex.
    *   *Compromise*: I will try simply adding `Segoe UI Emoji` to the priority list first. If that doesn't work, I'll use a safer set of characters or force the font for specific elements.

**Let's start with the specific override for the new water/snow icons**, as those are purely emojis and most likely to fail.

### Execution Plan
1.  **Modify `sounding_plotter.py`**:
    *   Update `__init__` to include `Segoe UI Emoji` in the fallback list.
    *   Update `_plot_simple`: For the "Cloud/Precip" column (💧/❄️), explicitly set `fontname='Segoe UI Emoji'`.
    *   For the status icons (🏃‍♂️ etc.), also try forcing the font or using simpler Unicode characters if necessary.

I will prioritize adding `Segoe UI Emoji` to the font list and forcing it for the droplet/snowflake icons.
