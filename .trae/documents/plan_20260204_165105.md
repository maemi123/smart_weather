I will fix the `TypeError: Object of type int32 is not JSON serializable` by explicitly converting NumPy types to native Python types in `history_analyzer.py`.

### 1. Identify Problematic Areas
The error occurs because pandas operations (like `.sum()`, `.mean()`) and NumPy calculations (like `lstsq`) return NumPy scalars (e.g., `numpy.int32`, `numpy.float64`) instead of native Python `int` or `float`. Flask's JSON encoder does not handle these NumPy types by default.

Key areas to fix in `history_analyzer.py`:
*   **`analyze_trend`**: Convert `year` (from `years`) to `int`.
*   **`_calculate_linear_regression`**: Convert `slope`, `intercept`, and `rate_per_decade` to `float`.
*   **`_calculate_yearly_stats`**: Convert sums (e.g., `hot_days`, `rainy_days`) to `int`.

### 2. Implementation Plan
I will modify `history_analyzer.py` to add explicit type casting:

*   In `get_available_years`:
    ```python
    years = sorted(self.data['year'].unique())
    return [int(y) for y in years]  # Convert to Python int
    ```
*   In `_calculate_linear_regression`:
    ```python
    return {
        "slope": float(m),
        "intercept": float(c),
        "rate_per_decade": float(round(m * 10, 2))
    }
    ```
*   In `_calculate_yearly_stats`:
    ```python
    stats["hot_days"] = int((daily_max >= 35).sum())
    stats["cold_days"] = int((daily_min <= 0).sum())
    stats["rainy_days"] = int((daily_precip['precipitation_corrected'] >= 0.1).sum())
    stats["heavy_rain_days"] = int((daily_precip['precipitation_corrected'] >= 50).sum())
    # ...
    stats["snow_days"] = int(snow_days_count)
    # ...
    stats["sunshine_hours"] = int(...)
    ```

### 3. Execution
I will apply these changes using `SearchReplace` on `history_analyzer.py` and then restart the application to verify the fix.
