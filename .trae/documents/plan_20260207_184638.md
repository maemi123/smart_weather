I will implement the requested enhancements for the Sounding Analysis Module.

### 1. Raw Data Display & Copy

* **Frontend**: Add a "View Raw Data" button in the control panel.

* **Modal**: Implement a Bootstrap modal to display the raw text data (CSV format).

* **JS**: Add functionality to fetch the raw CSV content and copy it to the clipboard.

* **Backend**: Ensure the API returns or exposes the raw CSV content URL.

### 2. Indices Enhancements

* **Rename**: Change "Precipitable Water" to "Total Precipitable Water" (整层可降水量).

* **Tooltips/Collapse**: Add an info icon or click-to-expand feature for each index to show a plain-language explanation (e.g., "CAPE: Energy available for storms. >2000 is high.").

### 3. Redesign "Cartoon Simple Plot"

* **Concept**: Replace the current simple icon plot with a **Vertical Profile Health Report** style.

* **Implementation**: Rewrite `_plot_simple` in `sounding_plotter.py` to use Matplotlib to draw a structured report.

  * **Layout**: Vertical axis represents height (Surface -> 10km+).

  * **Sections**:

    * **High Altitude (10km+)**: Jet stream/Wind check.

    * **Mid Level (5km)**: Dryness check (Dewpoint depression).

    * **Freezing Level (0°C)**: Height marker.

    * **Inversion Layer**: Check for temp increase with height.

    * **Low Level (Surface)**: Humidity/Fog check.

  * **Visuals**: Use text annotations, colored bars (red/yellow/green) on the side, and simple markers.

  * **Text**: "Diagnosis" summary at the bottom.

  * Add the original cartoon graphic to the bottom-right corner of the chart.

### Execution Plan

1. **Modify** **`sounding_plotter.py`**: Completely rewrite `_plot_simple` to generate the "Atmospheric Health Report" infographic.
2. **Modify** **`templates/upperair.html`**:

   * Add the "Raw Data" button and Modal.

   * Update the indices list with tooltips/collapse details.

   * Update the label for Precipitable Water.
3. **Modify** **`static/js/upperair.js`**:

   * Handle the raw data fetch and copy logic.

   * Update UI bindings if necessary.

I will start by redesigning the plotter as it's the most complex task.
