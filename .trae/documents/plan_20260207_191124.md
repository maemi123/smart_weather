I will address the user's feedback by fixing the layout bug in the simple plot, localizing the chart title, and enhancing the raw data modal.

### 1. Fix Layout Overlap in "Atmospheric Health Report" (`sounding_plotter.py`)

*   **Problem**: The "0°C Level" and "Inversion Layer" text blocks are hardcoded at fixed Y-positions (`y_freeze = 0.5`, `y_inv = 0.35`). When the actual physical heights are close (e.g., freezing level at 330m vs. inversion at 1070m), the fixed visual order (0°C above Inversion) contradicts reality, and they might overlap or appear confusing.
*   **Solution**:
    *   Implement a dynamic layout system. Instead of fixed Y-coordinates, I will sort the layers (High, Mid, Freezing, Inversion, Surface) based on their actual height/pressure importance or simply adjust the visual stack order.
    *   **Simpler Fix**: Since this is a schematic diagram, I will swap the visual positions of "0°C Level" and "Inversion Layer" if their heights are inverted in reality (Freezing < Inversion). Or better, I'll ensure they have enough vertical spacing and reflect the relative order if possible, but given the fixed schematic nature, I will at least add a check:
        *   If `freezing_hght < inversion_hght`, I will swap their display Y-coordinates or adjust them to avoid logical visual conflict.
        *   Actually, a "health report" list doesn't strictly need to be a height-scaled chart. I will keep the fixed slots but label them clearly, or better, simply swap the Y-positions if `freezing_hght < inversion_hght` to maintain logical consistency (higher features should appear higher).

### 2. Localize Chart Title (`sounding_plotter.py`)

*   **Problem**: Title shows "Atmospheric Health Report (Hangzhou ... 12Z...)".
*   **Solution**:
    *   Convert the UTC time string to a Chinese formatted string (e.g., "2024年X月X日 20时").
    *   Map station ID "58457" to "杭州".
    *   Update the `ax.text` call for the title.

### 3. Enhance Raw Data Modal (`static/js/upperair.js` & `app.py`)

*   **Problem**: Currently only shows a summary. User wants "Full Data".
*   **Solution**:
    *   **Backend (`app.py`)**: The `/upperair/data` endpoint already returns parsed JSON. I need to ensure it includes the **full list** of levels, not just indices.
    *   **Frontend (`upperair.js`)**:
        *   Instead of constructing a summary string, I will iterate through *all* levels in the JSON response.
        *   Format it as a CSV-like or aligned text table (Pressure, Height, Temp, Dewpoint, Wind...) so it looks like the original raw data.
        *   This avoids needing a separate API call if the JSON payload is sufficient (which it likely is).

### Execution Plan

1.  **Modify `sounding_plotter.py`**:
    *   Add logic to format the date/time to Chinese.
    *   Add logic to swap `y_freeze` and `y_inv` if `freezing_hght < inversion_hght`.
2.  **Modify `static/js/upperair.js`**:
    *   Update the "View Raw Data" logic to generate a full CSV-formatted string from the `result.data.levels` array (I need to check if `levels` is passed in the response; if not, I'll update `app.py`).
3.  **Check `app.py`**: Ensure `get_upperair_data` returns the full `levels` list.

I will start with the plotter fixes.
