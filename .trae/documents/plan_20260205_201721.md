I will fix the missing indices (LI and 0-6km Shear) and enhance the AI analysis report as requested.

### 1. Fix Missing Indices in `sounding_analyzer.py`

*   **0-6km Wind Shear (`SHEAR_06KM`)**:
    *   Currently, the code doesn't implement a MetPy calculation fallback for shear.
    *   I will implement logic to calculate bulk shear between the surface and 6km height using `metpy.calc.bulk_shear`.
    *   Logic: Find the index for surface and 6km (or nearest), extract wind components (u, v), and calculate the shear vector magnitude.

*   **Lifted Index (`LIFTED_INDEX`)**:
    *   The current implementation tries `mpcalc.lifted_index(p, T, Td, parcel_prof)`.
    *   I need to ensure the units are stripped correctly (`.magnitude`) and handle the return format properly (it returns an array, so I need to access the first element).

### 2. Enhance AI Analysis Report

*   **Data Extraction**:
    *   I will add a helper method `_get_layer_data(data)` to extract temperature, dewpoint, humidity, and wind at key pressure levels (Surface, 925, 850, 700, 500 hPa).
    *   This provides the AI with a 3D view of the atmosphere, not just stability indices.

*   **Prompt Engineering**:
    *   I will significantly expand the system prompt in `_generate_ai_report`.
    *   **New Sections**:
        *   **Layer Data**: Pass the extracted level data.
        *   **Broader Scope**: Ask the AI to analyze:
            *   **Precipitation Type**: Rain vs. Snow (based on freezing level and temp profile).
            *   **Cloud/Visibility**: Based on humidity at different levels.
            *   **Comfort**: General feeling.
            *   **Fair Weather**: If stable, describe the quality of the day.
    *   **Structure**: Update the JSON output requirements to include these new insights.

### Execution Plan

1.  Modify `sounding_analyzer.py`:
    *   Update `_calculate_parameters` to add 0-6km shear calculation.
    *   Add `_get_layer_data` method.
    *   Update `_generate_ai_report` with the new prompt and data.
2.  Verify the fix by checking if indices are populated and the AI report is richer.
