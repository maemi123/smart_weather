I will fix the data fetching bug and enhance the simple plot as requested.

### 1. Fix "Future Data" Fetching Bug

*   **Problem**: The current `get_latest_valid_time` logic aggressively rounds to the nearest 12-hour interval (00Z or 12Z).
    *   If it's 21:00 Beijing Time (13:00 UTC), it rounds to 12:00 UTC (Today 12Z). This is correct *if* data is published instantly, but usually there's a delay (e.g., 1-2 hours).
    *   If it's 20:00 Beijing Time (12:00 UTC), it requests 12:00 UTC immediately. Wyoming likely hasn't published it yet, leading to a 404/Empty response.
*   **Solution**:
    *   Modify `fetch_sounding_data` in `sounding_parser.py`.
    *   **Retry Logic**: If the request for `target_time` fails (e.g., 404 or empty data), automatically **fallback** to the *previous* valid time (minus 12 hours).
    *   This ensures that if the "latest" data isn't out yet, the user gets the most recent *available* data (e.g., 00Z instead of 12Z) without an error.

### 2. Enhance "Simple Plot" with Humidity Visuals

*   **Goal**: Add visual indicators (water droplets 💧 or snowflakes ❄️) on the "Atmospheric Health Report" chart where humidity is near saturation.
*   **Implementation**:
    *   In `sounding_plotter.py` -> `_plot_simple`:
    *   Iterate through pressure levels (e.g., every 50hPa or key levels).
    *   Check `RELH` (Relative Humidity). If `RELH > 90%` (or Dewpoint Depression < 2°C):
        *   Determine precip type based on Temperature (T > 0°C ? 💧 : ❄️).
        *   Plot small scatter points or text icons at the corresponding height on the chart background (e.g., randomly distributed or along a vertical line).
    *   **Refinement**: To keep it clean, maybe just add a "Cloud Layer" visual block or icons next to the vertical axis where RH is high.
    *   **Selected Approach**: I will add a visual "Cloud/Precip Column" on the right side of the chart.
        *   Scan the profile.
        *   Where RH > 85%, draw cloud/rain icons.
        *   Where T < 0, use snow icons.

### Execution Plan

1.  **Modify `sounding_parser.py`**:
    *   Update `fetch_sounding_data` to include a retry mechanism: `if response fails and target_time was auto-generated -> try target_time - 12h`.
2.  **Modify `sounding_plotter.py`**:
    *   Update `_plot_simple` to draw humidity icons (💧/❄️) at heights where the atmosphere is saturated.

I will start with the parser fix to ensure data stability.
