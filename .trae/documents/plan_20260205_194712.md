I will fix the `AttributeError` by updating the deprecated MetPy method names in `sounding_plotter.py`.

### 1. Identify Deprecated Methods
The error `'SkewT' object has no attribute 'plot_dryadiabats'` confirms that the code is using the old MetPy API (pre-1.0). The following methods need to be renamed to their snake_case versions:

*   `plot_dryadiabats` -> **`plot_dry_adiabats`**
*   `plot_moistadiabats` -> **`plot_moist_adiabats`**

### 2. Implementation Plan
I will use `SearchReplace` to modify `sounding_plotter.py`:

*   **Change 1**: Replace `skew.plot_dryadiabats(...)` with `skew.plot_dry_adiabats(...)`.
*   **Change 2**: Replace `skew.plot_moistadiabats(...)` with `skew.plot_moist_adiabats(...)`.

This update will make the code compatible with the currently installed version of MetPy.
