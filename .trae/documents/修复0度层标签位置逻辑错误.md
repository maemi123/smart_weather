# 修复0度层标签位置逻辑错误

## 问题
当前代码使用 `min` 导致Y值变小，位置往下，但用户需要往上。

## 当前错误代码
```python
y_freeze_label = min(y_freeze_label, y_mid_bottom + 0.06)
```

## 修复
改为 `max`，让Y值变大，位置往上：
```python
y_freeze_label = max(y_freeze_label, y_mid_bottom + 0.06)
```

## 涉及文件
- sounding_plotter.py
