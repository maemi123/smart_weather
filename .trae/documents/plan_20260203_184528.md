I will optimize the Advanced Forecast interface and logic as follows:

### 1. Backend Logic Optimization (`advanced_forecast_service.py`)

* **Fix Weather Description Logic**: Rewrite `_generate_weather_desc` to use actual `weather_code` (weather phenomena) instead of guessing based on temperature.

* **Correct Precipitation Thresholds**:

  * Update rain intensity standards: **>25mm** as Heavy Rain (大雨), **>50mm** as Torrential Rain (暴雨).

* **Implement Solid Precipitation Logic**:

  * Add logic to detect Snow vs. Sleet.

  * Rule: If solid precipitation (snow codes) occurs and Minimum Temperature > 0°C → Display as "**Sleet**" (e.g., Heavy Sleet / 大雨夹雪).

  * Rule: If pure snow codes → Display as "**Snow**" (e.g., Heavy Snow / 大雪).

* **Data Aggregation**: Update `generate_10day_summary` to collect and analyze daily `weather_code` statistics to support the above logic.

### 2. Frontend Interface Optimization (`templates/advanced_forecast.html`)

* **72-hour Precipitation Chart**:

  * Enhance the Bar Chart logic to check `weather_code` for each hour.

  * **Visual Distinction**: If the weather code indicates solid precipitation (Snow/Ice), set the bar color to **Light Blue** (`#87CEFA`) to distinguish it from liquid rain (Standard Blue `#3498db`).

### Implementation Steps

1. Modify `advanced_forecast_service.py` to implement the new weather description logic and thresholds.
2. Modify `templates/advanced_forecast.html` to update the Chart.js/Plotly rendering logic for the precipitation bars.

