I will fix the weather description logic and add new models for comparison.

### 1. Fix "Snow Day Showing as Heavy Rain" Issue
The current logic in `_generate_weather_desc` prioritizes precipitation amount over weather type when generating the description. If the precipitation is high (e.g., >10mm), it defaults to "Heavy Rain" (大雨) unless the *daily mode* weather code explicitly matches snow. However, sometimes the daily mode might not capture the snow event if it's mixed with rain or if the mode calculation is skewed.

**Optimization Plan:**
*   **Modify `_generate_weather_desc`**:
    *   Pass `temp_min` (minimum temperature) to this function.
    *   **Logic Change**: Even if the precipitation is high (leading to "Heavy/Torrential" intensity), we must strictly check the `weather_code`.
    *   **Crucial Fix**: If the `weather_code` indicates snow (71-86) OR if the `temp_min` is low enough (e.g., < 2°C) combined with precipitation, we should bias towards "Snow" or "Sleet" instead of just "Rain".
    *   Specifically, if `weather_code` is missing or ambiguous but `temp_min < 0` and there is precipitation, force "Snow" (雪) suffix.
    *   If `temp_min` is between 0-2°C, default to "Sleet" (雨夹雪) to be safe.

### 2. Add GFS and ICON Models
The user wants to add `gfs_seamless` and `icon_global` to the multi-model comparison.

**Optimization Plan:**
*   **Modify `fetch_multi_model_forecast`**:
    *   Update the `target_models` list to include `gfs_seamless` and `icon_global`.
    *   Current list: `["best_match", "ecmwf_ifs"]`
    *   New list: `["best_match", "ecmwf_ifs", "gfs_seamless", "icon_global"]`

### Implementation Steps
1.  **Modify `advanced_forecast_service.py`**:
    *   Update `fetch_multi_model_forecast` to include new models.
    *   Refine `_generate_weather_desc` to handle the snow/rain distinction more intelligently based on temperature thresholds when codes are ambiguous, ensuring "Snow" is displayed when appropriate.

2.  **Verify**:
    *   Restart the app.
    *   Check the "Multi-Model Comparison" tab to see if GFS and ICON are loaded.
    *   Check the "7-Day Trend" to see if the Feb 6th weather description updates from "Heavy Rain" to "Heavy Snow" or "Heavy Sleet".
