# 修复中层风险显示和逆温层显示逻辑

## 问题1: 中层"雷暴大风风险"显示逻辑

### 当前错误代码 (sounding_plotter.py 第285-296行)
```python
is_dry = mid_depression > 15
...
risk_icon = "⚡" if is_dry else "☁️"
risk_text = "雷暴大风风险增高" if is_dry else "云层较厚"
```

### 修复方案（细化版）
```python
is_dry = mid_depression > 15

# 需要传入cape参数
if is_dry and cape > 1500:
    risk_icon = "⚡"
    risk_text = "对流能量充足且温露差大，雷暴大风风险增高"
elif is_dry:
    risk_icon = "☀️"
    risk_text = "空气干燥，云层较少"
else:
    risk_icon = "☁️"
    risk_text = "湿度较高，云层可能发展"
```

### 需要修改
1. 在函数参数中添加 `cape` 参数
2. 修改中层风险判断逻辑

## 问题2: 逆温层虚线不显示但文字显示

### 问题代码 (sounding_plotter.py 第327-342行)
```python
# D. 逆温层 (如果有)
if has_inversion:
    if min_hght < inversion_hght < max_hght:
        ax.plot([0.18, 0.8], [y_inv_real, y_inv_real], ls=':', color='#d35400', lw=2)
        # ... 虚线和标签 ...
    
    ax.text(0.25, y_inv_label-0.03, "特性: 温度随高度上升 (反常现象)", fontsize=10)
    # ... 其他文字 ...
```

**问题**: 第337行开始的文字代码在 `if has_inversion:` 块内，但不在 `if min_hght < inversion_hght < max_hght:` 条件内，导致逆温层不在显示范围时文字仍然显示。

### 修复方案
将所有逆温层相关的文字移到 `if min_hght < inversion_hght < max_hght:` 条件内：

```python
# D. 逆温层 (如果有)
if has_inversion and min_hght < inversion_hght < max_hght:
    ax.plot([0.18, 0.8], [y_inv_real, y_inv_real], ls=':', color='#d35400', lw=2)
    ax.annotate(...)
    draw_icon_text(...)
    ax.text(0.25, y_inv_label-0.03, "特性: 温度随高度上升 (反常现象)", fontsize=10)
    ax.text(0.25, y_inv_label-0.06, "→ 对您意味着:", fontsize=10, color='#e67e22', style='italic')
    draw_icon_text(0.38, y_inv_label-0.06, "😷", "污染物堆积，雾霾难散", '#e67e22', fontsize=10)
    self._draw_status_bar(ax, 0.85, y_inv_label-0.03, 0.8, 'orange')
```

## 实施步骤

1. 修改 `_draw_intuitive_panel` 函数签名，添加 `cape` 参数
2. 修改中层风险判断逻辑
3. 修改逆温层显示条件，确保虚线和文字同时显示/隐藏
4. 检查调用该函数的地方，确保传入cape参数

## 涉及文件
- sounding_plotter.py
